import 'package:flutter/cupertino.dart';

class CustomViewCustomerRateDesignWidget extends StatelessWidget {
  const CustomViewCustomerRateDesignWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      spacing: 10,
      children: [

      ],
    );
  }
}
